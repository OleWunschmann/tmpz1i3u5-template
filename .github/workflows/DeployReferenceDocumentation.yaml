name: ' Deploy Reference Documentation'

on:
  workflow_dispatch:

permissions:
  actions: read
  contents: read
  id-token: write
  pages: write

defaults:
  run:
    shell: pwsh

env:
  ALGoOrgSettings: ${{ vars.ALGoOrgSettings }}
  ALGoRepoSettings: ${{ vars.ALGoRepoSettings }}

jobs:
  DeployALDoc:
    runs-on: [ ubuntu-latest ]
    name: Deploy Reference Documentation
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Initialize the workflow
        id: init
        uses: OleWunschmann/AL-Go/Actions/WorkflowInitialize@update-with-customALGoFiles-of-template
        with:
          shell: pwsh

      - name: Read settings
        uses: OleWunschmann/AL-Go/Actions/ReadSettings@update-with-customALGoFiles-of-template
        with:
          shell: pwsh

      - name: Determine ArtifactUrl
        id: determineArtifactUrl
        uses: OleWunschmann/AL-Go/Actions/DetermineArtifactUrl@update-with-customALGoFiles-of-template
        with:
          shell: pwsh

      - name: Determine Deployment Environments
        id: DetermineDeploymentEnvironments
        uses: OleWunschmann/AL-Go/Actions/DetermineDeploymentEnvironments@update-with-customALGoFiles-of-template
        env:
          GITHUB_TOKEN: ${{ github.token }}
        with:
          shell: pwsh
          getEnvironments: 'github-pages'
          type: 'Publish'

      - name: Setup Pages
        if: steps.DetermineDeploymentEnvironments.outputs.deployALDocArtifact == 1
        uses: actions/configure-pages@45bfe0192ca1faeb007ade9deae92b16b8254a0d # v6.0.0

      - name: Build Reference Documentation
        uses: OleWunschmann/AL-Go/Actions/BuildReferenceDocumentation@update-with-customALGoFiles-of-template
        with:
          shell: pwsh
          artifacts: 'latest'
          artifactUrl: ${{ env.artifact }}

      - name: Upload pages artifact
        uses: actions/upload-pages-artifact@fc324d3547104276b827a68afc52ff2a11cc49c9 # v5.0.0
        with:
          path: ".aldoc/_site/"

      - name: Deploy to GitHub Pages
        if: steps.DetermineDeploymentEnvironments.outputs.deployALDocArtifact == 1
        id: deployment
        uses: actions/deploy-pages@cd2ce8fcbc39b97be8ca5fce6e763baed58fa128 # v5.0.0

      - name: Finalize the workflow
        if: always()
        uses: OleWunschmann/AL-Go/Actions/WorkflowPostProcess@update-with-customALGoFiles-of-template
        env:
          GITHUB_TOKEN: ${{ github.token }}
        with:
          shell: pwsh
          telemetryScopeJson: ${{ steps.init.outputs.telemetryScopeJson }}
          currentJobContext: ${{ toJson(job) }}